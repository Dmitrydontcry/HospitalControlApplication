<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Больница</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/patients">Пациенты</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/analyses">Анализы</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/attachments">Вложения</a>
        </li>
      </ul>
        <% if user_signed_in? %>
          <div class="me-2" style="color:white;">
            <%= current_user.name %>
          </div>
          <div class="me-2" style="color:white;">
            <%= current_user.email %>
          </div>
          <div>
            <%= form_tag(destroy_user_session_path, method: :delete) do %>
              <%= submit_tag 'Выход', class: "btn btn-primary" %>
            <% end %>
          <div>
        <% end %>
    </div>
  </div>
</nav>
